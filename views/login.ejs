<!DOCTYPE html>
<html lang="en">
    <%- include('partials/head') %>

    <body>

        <%- include('partials/header') %>


<main class="login-main">
<h1 class="inicioS">Iniciar Sesión</h1>
    
        <!--FORMULARIO-->
        <form action="/user/login" method="POST" class="login-form">
             
            <div class="control-login-form">
                <label for="nombreUsuario"></label>
                <input type="text" name="userName" placeholder="Nombre de Usuari@" id="nombreUsuario" >
                <% if(locals.errors && errors.userName){ %>  
                    <div class="text-danger">
                        <%= errors.userName.msg %>  
                    </div>
                    
                    
                        <%} %> 
            </div>
            <div class="control-login-form">
                <label for="contraseña"></label>
                <input type="password" name="password" placeholder="Contraseña" minlength="6" maxlength="15" id="contraseña">
                <% if(locals.errors && errors.password){ %>  
                    <div class="text-danger">
                        <%= errors.password.msg %> 
                    </div>
                    
                    
                        <%} %> 
            </div>

            <input type="checkbox" name="remember_user">Recordarme

            <div class="control-login-formBut">


                <button type ="submit">Ingresar</button>
            </div>
        </form>

    </main>

    <%- include('partials/footer') %>

    </body>